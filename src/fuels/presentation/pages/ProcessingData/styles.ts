import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center' },
  title: { fontSize: 18, textAlign: 'justify', marginHorizontal: 32 },
  animation: { marginHorizontal: 24 },
});
