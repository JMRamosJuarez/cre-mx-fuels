import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { overflow: 'hidden' },
  progress: { overflow: 'hidden', height: 8, borderRadius: 8 },
  bar: { height: '100%' },
  title: { marginVertical: 4, textAlign: 'center', fontSize: 18 },
});
